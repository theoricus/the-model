li(data-id="#{id}", data-cid="#{cid}", id="#{id}")
  .view(data-id="#{id}")
    input.toggle(type="checkbox", #{todo.keys.done})
    label #{keys.title}
    button.destroy
  input.edit(value="#{keys.title}")

